diff --git a/core.js b/core.js
index d653e66a4056c27cca777d4e25222acae3b2ec85..49f0ae7c35c65934f6ca2017d6b2a098650892ad 100644
--- a/core.js
+++ b/core.js
@@ -1076,6 +1076,12 @@ async function _fromTokenizer(tokenizer) {
 		while (tokenizer.position + 24 < tokenizer.fileInfo.size) {
 			const header = await readHeader();
 			let payload = header.size - 24;
+			if (payload < 0) {
+				// Malformed sub-header (size too small to be valid): bail out instead of
+				// looping forever with a negative `ignore()` that never advances position.
+				break;
+			}
+
 			if (_check(header.id, [0x91, 0x07, 0xDC, 0xB7, 0xB7, 0xA9, 0xCF, 0x11, 0x8E, 0xE6, 0x00, 0xC0, 0x0C, 0x20, 0x53, 0x65])) {
 				// Sync on Stream-Properties-Object (B7DC0791-A9B7-11CF-8EE6-00C00C205365)
 				const typeId = Buffer.alloc(16);
